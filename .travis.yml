language: python
python:
    - "3.6"

install:
    - pip install pipenv
    - pipenv install --dev

script:
    - make check
    - make test

after_script:
    - make coverage
    - pipenv run coveralls
